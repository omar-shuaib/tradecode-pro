"use client";import {DUTY_DISCLAIMER_TEXT} from "../lib/shared-types";import {jsPDF} from "jspdf";import * as XLSX from "xlsx";
export function ExportMenu({data}:{data:unknown}){function pdf(){const d=new jsPDF();d.text("TradeCode Pro classification report",10,15);d.text(JSON.stringify(data,null,2).slice(0,1200),10,30,{maxWidth:185});d.setFontSize(8);d.text(DUTY_DISCLAIMER_TEXT,10,280,{maxWidth:185});d.save("tradecode-report.pdf")}function csv(){const rows=[{notice:DUTY_DISCLAIMER_TEXT},{result:JSON.stringify(data)},{notice:DUTY_DISCLAIMER_TEXT}];const csv=XLSX.utils.sheet_to_csv(XLSX.utils.json_to_sheet(rows));const a=document.createElement("a");a.href=URL.createObjectURL(new Blob([csv],{type:"text/csv"}));a.download="tradecode-report.csv";a.click()}return <p><button onClick={pdf}>Export PDF</button> <button onClick={csv}>Export CSV</button> <button onClick={()=>navigator.clipboard.writeText(JSON.stringify(data,null,2))}>Copy JSON</button></p>}
