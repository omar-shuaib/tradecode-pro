"use client";import { useEffect } from "react";import { api } from "../lib/api";export function ErrorCapture(){useEffect(()=>{window.onerror=(_e,_s,_l,_c,error)=>void api.logError({route:location.pathname,message:String(error??_e),stack:error?.stack});window.onunhandledrejection=e=>void api.logError({route:location.pathname,message:String(e.reason),stack:e.reason?.stack})},[]);return null}
